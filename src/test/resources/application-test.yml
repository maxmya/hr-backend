spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:h2:mem:hr_backend_test
    username: test
    password: test
    driver-class-name: org.h2.Driver